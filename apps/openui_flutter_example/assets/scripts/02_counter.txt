$count = 0
root = Card([
  CardHeader("Reactive counter"),
  TextContent("Count: " + $count, "large-heavy"),
  Stack([
    Button("Increment", Action([@Set($count, $count + 1)]), "primary"),
    Button("Reset", Action([@Reset($count)]), "secondary")
  ], "row")
])
