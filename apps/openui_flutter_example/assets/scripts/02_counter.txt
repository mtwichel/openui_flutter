$count = 0
root = Card([
  CardHeader("Reactive counter"),
  TextContent("Count: " + $count, "large-heavy"),
  Stack([
    Button("Increment", [@Set($count, $count + 1)], "primary"),
    Button("Reset", [@Reset($count)], "secondary")
  ], "row")
])
