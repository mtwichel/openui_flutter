$name = ""
$role = "engineer"
root = Card([
  CardHeader("Sign-up form"),
  Form("signup", [
    FormControl("Display name", [Input("displayName", $name)]),
    FormControl("Role", [Select(["engineer", "designer", "pm"], $role)])
  ]),
  TextContent("Hello, " + $name + " (" + $role + ")"),
  Stack([
    Button("Reset", [@Reset($name, $role)], "secondary")
  ], "row")
])
