/// Width below which the app shell switches from a permanent
/// `NavigationRail` to a `Drawer`, and the live-chat screen switches
/// from side-by-side to stacked layout.
///
/// Both consumers read this constant so the two layouts flip at the
/// same viewport width.
const double kWideBreakpoint = 900;
