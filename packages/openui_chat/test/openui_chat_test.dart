// Phase 0 placeholder. The OpenUiChatController and adapter tests land in
// Phase 3 alongside the implementation.

// The barrel is intentionally empty in Phase 0; importing it here is the
// only way to assert the public surface compiles.
// ignore: unused_import
import 'package:openui_chat/openui_chat.dart';
import 'package:test/test.dart';

void main() {
  test('Phase 0 scaffold compiles', () {
    expect(1 + 1, 2);
  });
}
