// Phase 0 placeholder. Per-component widget and golden tests land in Phase 3.

import 'package:flutter_test/flutter_test.dart';
// The barrel is intentionally empty in Phase 0; importing it here is the
// only way to assert the public surface compiles.
// ignore: unused_import
import 'package:openui_components/openui_components.dart';

void main() {
  test('Phase 0 scaffold compiles', () {
    expect(1 + 1, 2);
  });
}
