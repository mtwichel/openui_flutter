/// MCP tool provider for OpenUI Flutter.
///
/// This is the only file consumers should import from `openui_mcp`.
/// Every public symbol is currently marked `@experimental` — the shape
/// may change between v0.1 and v0.2.
library;

export 'src/mcp_tool_provider.dart' show McpCallToolFn, McpToolProvider;
