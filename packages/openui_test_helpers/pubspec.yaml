name: openui_test_helpers
description: Shared mocks, fakes, and golden helpers for OpenUI Flutter tests.
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.9.0

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.2.0
